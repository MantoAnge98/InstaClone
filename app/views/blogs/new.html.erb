<h2>New Post</h2>
  <% if logged_in? %>
    <%=render 'form' %>
  <% else %>
    <p>Log in : <%= link_to "Log in", new_session_path, class: "btn btn-secondary"%></p>
    <p>Or Sign Up : <%= link_to "Sign Up", new_user_path, class: "btn btn-secondary"%></p>
  <% end %>